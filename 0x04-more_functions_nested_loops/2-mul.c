#include "main.h"

/**
 * mul - to multiply two integers
 * @a: integer to be multiplied
 * @b: integer to be multiplied
 * Return: 0
 */

int mul(int a, int b)
{
	int a = 10;
	int b = 5;
	int mul;

	mul = a * b;

	return (mul);
}

int main(void)
{
	mul(a, b);

	return (0);
}
