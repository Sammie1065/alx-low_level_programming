#include <stdio.h>
/**
 * main - print all single digit num of base 10
 *
 * Return: Always 0 (correct)
 */
int main(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
